SELECT 
	FLEX_FIELD,
	ORDEM_TELA,
	DESCRICAO_TELA,
	TIPO_DADO,
	TAMANHO,
	NR_DECIMAIS,
	SQL_LISTA_VALORES
FROM
	LY_FLEX_FIELDS
WHERE
	TABELA IN ('LY_TURMA')
	
SELECT
	FLEX_FIELD, 
	ORDEM_TELA,
	DESCRICAO_TELA,
	TIPO_DADO,
	TAMANHO,
	NR_DECIMAIS,
	SQL_LISTA_VALORES
FROM
	LY_FLEX_FIELDS
WHERE
	TABELA IN ('LY_OFERTA_CURSO')

SELECT
	FLEX_FIELD, 
	ORDEM_TELA,
	DESCRICAO_TELA,
	TIPO_DADO,
	TAMANHO,
	NR_DECIMAIS,
	SQL_LISTA_VALORES
FROM
	LY_FLEX_FIELDS
WHERE
	TABELA IN ('LY_CONCURSO')
	
SELECT
	FLEX_FIELD, 
	ORDEM_TELA,
	DESCRICAO_TELA,
	TIPO_DADO,
	TAMANHO,
	NR_DECIMAIS,
	SQL_LISTA_VALORES
FROM
	LY_FLEX_FIELDS
WHERE
	TABELA IN ('LY_CURSO')

	
SELECT
	FLEX_FIELD, 
	ORDEM_TELA,
	DESCRICAO_TELA,
	TIPO_DADO,
	TAMANHO,
	NR_DECIMAIS,
	SQL_LISTA_VALORES
FROM
	LY_FLEX_FIELDS
WHERE
	TABELA IN ('LY_CANDIDATO')